# Workflow configuration

The workflow is configured via a YAML file located at `config/config.yaml`.

---

## Example

```yaml
datasets:
  test:
    mzmine:
      template: "dda_orbitrap_pos"
      retention_time_range: [0.5, 31]
      approximate_feature_fwhm: 0.1
      minimum_feature_height: 5e4
      blank_subtraction:
        min_blank_presence: 3
        fold_change_threshold: 3
    analysis:
      features:
        pca:
          samples_groupby: ["group", "fraction"]
          procedural_blanks_groupby: ["fraction"]
        da_condition_columns: ["fraction", "day_type"]
      annotations:
        formula_identification_score_cutoff: 0.8
        spectral_library_match_score_cutoff: 0.8
        structure_database_confidence_cutoff: 0.8
        msnovelist_score_cutoff: 0.8
      bioactivity:
        cc_spaces: ["C3", "C4", "D2", "D3"]
        query: "op"
        clusterable_embedding_umap_n_neighbors: 15
        clusterable_embedding_umap_min_dist: 0.0
        clusterable_embedding_umap_n_components: 5
        hdbdscan_min_samples: 5
        hdbscan_min_cluster_size: 10
```

---

## `datasets`

A map of datasets to process. Each key must match a subfolder name under `data/`.

### `mzmine`

MZmine processing settings for a dataset.

| Field | Type | Description |
|---|---|---|
| `template` | string | Name of the MZmine batch template to use as a base, currently only `dda_orbitrap_pos` is supported, but further templates can be places under `resources/templates` |
| `retention_time_range` | [number, number] | Start and end of the retention time window to process, in minutes. |
| `approximate_feature_fwhm` | number | Estimated peak width at half maximum, used to tune peak detection. |
| `minimum_feature_height` | number | Minimum intensity for a feature to be detected. |
| `blank_subtraction` | object | Settings for filtering out background signals (see below). |
| `blank_subtraction.min_blank_presence` | integer | Minimum number of blanks in which a feature must appear to be considered background. |
| `blank_subtraction.fold_change_threshold` | number | Minimum fold-change between sample and blank intensity for a feature to be retained. |

### `analysis`

Analysis settings for a dataset.

#### `features`

| Field | Type | Description |
|---|---|---|
| `pca.samples_groupby` | array | By which columns to group samples in PCA plot. |
| `pca.procedural_blanks_groupby` | array | By which columns to group procedural blanks in PCA plot. |
| `da_condition_columns` | array | What columns in metadata to use for differential analysis |

#### `annotations`

| Field | Type | Description |
|---|---|---|
| `formula_identification_score_cutoff`     | number (0-1, default 0.8) | Minimum threshold for accepting formula identifications. Only matches with scores ≥ cutoff are retained.  
| `spectral_library_match_score_cutoff`     | number (0-1, default 0.8) | Minimum similarity threshold for accepting spectral library matches. Based on normalized spectral similarity scores. Only matches with scores ≥ cutoff are retained.                                                                                                                      |
| `structure_database_confidence_cutoff` | number (0-1, default 0.8) | Minimum confidence threshold for accepting CSI:FingerID structure annotations. Applies to ConfidenceScoreExact. Higher values indicate stronger agreement between MS/MS data and the predicted structure.                                                                                      |
| `msnovelist_score_cutoff`   | number (negative, default -3) | Minimum score threshold for accepting de novo structures generated by MSNovelist. The score is typically negative, where values closer to 0 indicate higher confidence. More negative values indicate lower confidence. A higher (less negative) threshold is more strict, while a lower (more negative) threshold is more permissive. |

#### `bioactivity`

| Field | Type | Description |
|---|---|---|
| `cc_spaces` | array | Defines which Chemical Checker (CC) bioactivity spaces are used to compute similarity between compounds in dataset and query/reference compounds. Each space encodes a different level of biological abstraction, from chemical structure to clinical outcomes (see https://doi.org/10.1038/s41467-021-24150-4 and https://doi-org.ezproxy.library.wur.nl/10.1038/s41587-020-0502-7). |
| `query` | string | Name of the query set of smiles to use for bioactivity comparison, currently only `op` is supported, but further templates can be places under `resources/bioactivity_queries` |
| `clusterable_embedding_umap_n_neighbors` | integer (default 15) | The size of local neighborhood used for manifold approximation for clusterable emedding. |
| `clusterable_embedding_umap_min_dist` | float (default 0.0) | The effective minimum distance between embedded points for clusterable emedding. |
| `clusterable_embedding_umap_n_components` | integer (default 10) | The dimension of the space to embed into for clusterable emedding.  |
| `hdbdscan_min_samples` | integer (default 5) | The minimum size of clusters for HDBSCAN |
| `hdbdscan_hdbscan_min_cluster_size` | integer (default 10) | The number of samples in a neighborhood for a point to be considered as a core point for HDBSCAN. |
